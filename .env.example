# Copy to .env and fill in real values. .env is gitignored.
# Add new vars here (with empty values) as classes introduce them.

# LLM providers
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Observability (add as needed)
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=
