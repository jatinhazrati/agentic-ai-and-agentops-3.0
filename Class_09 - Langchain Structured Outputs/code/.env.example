# Copy to .env in this folder and fill in real values. .env is gitignored.
# Loaded by the notebook via load_dotenv(). Class 09 uses OpenAI (+ OpenRouter optional).
OPENAI_API_KEY=
OPENROUTER_API_KEY=
