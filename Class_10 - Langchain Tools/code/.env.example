# Copy to .env in this folder and fill in real values. .env is gitignored.
# Loaded by the notebook via load_dotenv(). Class 10 uses OpenAI + Tavily (+ OpenRouter optional).
OPENAI_API_KEY=
OPENROUTER_API_KEY=
TAVILY_API_KEY=
